# K90PM Tuner APP · 代码健康检查报告（性能 / 手机 / 系统影响）

> 检查日期：2026-08-07
> 对象：Tuner_APP/K90PM_Tuner（Compose APP v2.0.0）
> 说明：本报告**只做检查分析，不修改代码**，供决策参考。

---

## 一、总体结论

- 未发现 **AlarmManager / WorkManager / JobScheduler / 传感器 / WakeLock** 类后台唤醒（好）。
- 但存在 **1 个常驻前台服务 + 多处"每次进后台/每次调参"的 root/SHM 写操作**，会在高频使用时对系统资源和电量造成可感知负担，存在**潜在 ANR / 卡顿 / 多余耗电**风险。
- 目标 SDK 高（37）、release 未开 R8 压缩，属工程优化项（不影响运行安全）。

---

## 二、高风险项（建议重点评估）

### 🔴 H1. `AudioChainService` — 常驻前台服务（电量/资源占用主因）
文件：`music/AudioChainService.kt`
- 声明 `START_STICKY` + `startForeground()`，**进程一旦启动就不再释放**（onDestroy 才会 releaseChain）。
- 用**反射**创建/连接 `AudioEffect`（DAP_offload / MiSound / DynamicsProcessing）三条音频链，并强制 `dapEffect.setEnabled(true)` 作为"HAL 锚点"。
- **问题**：
  - START_STICKY 被系统杀掉后会自动重建，**无法主动退出**，长期驻留后台。
  - 持有 3 个 AudioEffect/DynamicsProcessing 原生对象，持续连接 audio HAL。
  - `DspManager.init()` 在 **App 启动即无条件 `startForegroundService`**（`DspManager.kt:55`）。
- **影响**：后台常驻占内存 + 持续音频系统连接；用户不使用时仍在跑。
- **建议**：改为**按需启动**（进调音页/播放时才起，退出即 stopForeground+stopSelf），或用 `super.onCreate` 里判断模块是否启用来决定是否常驻。

### 🔴 H2. `MainActivity.onStop` — 每次进后台都执行 root 写快照
文件：`app/MainActivity.kt:65-69` → `V4ATunerManager.saveExitSnapshot()`
- `onStop()` 每次 APP 退到后台都会：
  1. 再次 `V4ATunerManager.init(applicationContext)`（重复初始化，含可能的重建 root/session）
  2. `saveExitSnapshot()`：聚合**所有**功能参数 + 主控 status → 写 3 个 SHM 快照到 `/data/local/tmp/v4a/`
  3. 每个文件 `writeSnapshotFile` 里执行 `Runtime.getRuntime().exec("su -c chmod...; chcon...")` 且 **`waitFor()` 阻塞**
- **问题**：`onStop` 在**主线程**执行，而这里含 root 子进程 + 阻塞 waitFor + 文件 IO，**可能造成退后台卡顿/掉帧，极端下 ANR**；且频繁 root 子进程消耗。
- **建议**：改为只在**真正退出**（`onDestroy`）或**特定动作**（停止播放/手动保存）时写，且放到 `Dispatchers.IO`，并对 su 加超时/防重复。

### 🟠 M1. `ConfigChannel.writeViaSu` — 每次调参 fork su 进程
文件：`v4a/ConfigChannel.kt:456-469`
- 每次参数写回（调滑杆、切预设）时 `Runtime.getRuntime().exec("cat > tmp") + mv + chmod`，**每条参数一次 su 子进程**。
- **问题**：拖滑杆/切预设高频触发时，会**反复拉起 su + 写文件**，产生瞬时 CPU/IO 峰值，且 `/data/local/tmp` 频繁写。
- **建议**：对写操作做**节流/合并**（如 last-write-wins、进入空闲再 flush），避免 move 太频繁。

### 🟠 M2. `DspManager.init` 启动即拉起前台服务
文件：`music/DspManager.kt:46-60`
- `init()` 每次被调都 `startForegroundService`，与音频链常驻叠加放大后台负担。
- **建议**：与 H1 一并改造为按需/带开关。

---

## 三、中风险项

### 🟡 L1. `ExtModeScreen` 每 2 秒轮询 MediaSession
文件：`ui/screens/ExtModeScreen.kt:53-64`
- `LaunchedEffect` 里 `while(true){ ... delay(2000) }` 每 2 秒 `su dumpsys media_session`。
- **问题**：仅在该页面显示时运行（离开即取消），但**每 2 秒 su dumpsys 是较大开销**；且需 root。
- **建议**：把轮询间隔放大（如 5s），或用 MediaSessionManager 注册回调替代轮询。

### 🟡 L2. `V4AStartupRestoreService` 开机 `Thread.sleep(500)` 循环
文件：`v4a/V4AStartupRestoreService.kt:102`
- 开机恢复脚本用 `Thread.sleep(500)` 自旋等待就绪（配合模块 service.sh）。
- **风险**：若条件不满足会长时间占线程；建议设**总超时上限**（如最长 10s 后放弃）避免空转。

### 🟡 L3. 每次调参写 SharedPreferences + JSON
文件：`service/V4ATunerManager.kt`（多处 `saveCurrentConfig()` / `saveExitSnapshot`）
- 每次改动 `apply()` 全量 JSON。频度可控时无碍；高频拖条会重复序列化。
- **建议**：对 saveCurrentConfig 做 **debounce**（如 500ms），避免拖条时反复全量序列化。

### 🟡 L4. `writeViaSu` 与 SHM 的 `Runtime.exec` 混用 root
- APP 多处 `Runtime.getRuntime().exec("su ...")`（V4ATunerManager / V4ShmWriter / ConfigChannel / RootShell / WsaShell）。
- **问题**：每次新建 su 进程 + 管道 IO，无连接池。低频可接受，高频（调参、onStop）会叠加。
- **建议**：有长驻需求时用 **libsu 单会话 Shell**（依赖里已注明 libsu 思路），复用一个 root 进程，显著降开销。

---

## 四、工程/兼容性项（低风险但建议修）

- **release 未开 `isMinifyEnabled`**：`build.gradle.kts` release 为 `isMinifyEnabled=false` → APK 体积大、字符串未混淆（含 root 路径/命令明文）。不建议强制 R8（可能破坏反射），但可考虑 `isShrinkResources` 或加规则。
- **反射 AudioEffect 私有构造**（`AudioChainService`/`ViperEffect`）：依赖隐藏/私有 API，**不同厂商/Android 版本可能抛异常**（代码已 catch 但会退化功能）。建议加版本/厂商判断兜底。
- **`Class.forName("com.k90pm.tuner.app.MainActivity")`**：硬编码类名，若混淆/改名会崩（当前 release 未混淆所以没事，开 R8 后需注意）。
- **NDK 仅 `arm64-v8a`**：`abiFilters` 只有 arm64，符合 K90PM（arm64），没问题，但注意别在为 x86 测试设备打包时报错。
- **Coil ImageLoader crossfade + DiskCache**：合理，但壁纸图若过大建议限制尺寸防止内存峰值（comp 图可设 `size`）。
- **network 请求用 `URL.openConnection()`（HttpURLConnection）**：OK，但未看到统一超时/重试封装，弱网下界面可能长时间阻塞（若在主线程）。确认都在 IO 调度即可。

---

## 五、优先修复建议（按性价比）

1. **AudioChainService 改"按需常驻 + 可主动停止"** —— 最大电量/资源收益。
2. **onStop 的 root 快照写改到 IO + 防重复 + 只在真实退出/停止播放时** —— 消除退后台卡顿与 ANR 风险。
3. **SHM 写参数做节流合并（debounce）** —— 高频调参时平稳。
4. **root 操作收敛到 libsu 单会话** —— 减少 su 子进程开销。
5. 轮询类（ExtMode）降频 / 用回调替代。

---

## 六、附：本次已确认无问题的项
- 无 AlarmManager / WorkManager / JobScheduler 等后台唤醒 ✓
- 无 WakeLock / 传感器常开 ✓
- 未直接改系统 Settings（仅 SharedPreferences 应用内存储）✓
- `while(true)` 均为 Composable `LaunchedEffect` 内协程（页面机制，离开自动取消）✓
